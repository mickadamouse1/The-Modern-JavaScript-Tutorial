// use strict ensures that all of the code is read as modern code rather than old outdated code

"use strict";

// browser compatible method of using "use strict" :

window.onload = () => {
    "use strict"
}

